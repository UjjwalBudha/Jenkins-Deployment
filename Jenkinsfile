pipeline {
    agent any
        stage ('Build') {
            steps {
                sh 'pwd'
                sh 'touch a.txt'
                sh 'ls -ltr'
                sh 'whoami'
                sh 'sudo docker ps'
            }
        }
    }
